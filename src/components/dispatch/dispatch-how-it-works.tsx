export function DispatchHowItWorks() {
  return (
    <section className="dispatch-section">
      <h2 className="dispatch-section-label">How it works</h2>
      <div className="space-y-4 text-sm text-text-mute leading-relaxed">
        <p>
          Stoa Dispatch is a daily digest of the highest-signal analyst calls on the platform —
          ranked by conviction, recency, and track record.
        </p>
        <p>
          Each issue covers a 24-hour window in New York time. Lead stories get the full treatment;
          everything else runs in the wire below.
        </p>
        <p>
          When predictions resolve, they land in Today&apos;s Record with a sealed outcome and an
          updated moat score.
        </p>
      </div>
    </section>
  );
}
